// const minBtn = document.querySelectorAll('.minus-btn');
// const plusBtn = document.querySelectorAll(".plus-btn")
// const value = document.querySelectorAll(".quantity");
// const price = document.querySelectorAll('.cart_price');
// const total = document.querySelectorAll('.cart_total');

// console.log(parseInt(price[0].textContent.trim().slice(2)));
// minBtn[0].setAttribute("disabled", "disabled");
// const btn = document.querySelectorAll(".color");

// minBtn.forEach((el, idx) => {
//     let count = parseInt(value[idx].textContent);
//     plusBtn[idx].addEventListener("click", function() {
//         count++;
//         if (count > 1) {
//             minBtn[idx].removeAttribute("disabled");
//             minBtn[idx].classList.remove("disabled")
//         }
//         value[idx].textContent = count;
//         total[idx].textContent = count * parseInt(price[0].textContent.trim().slice(2))
//     });

//     minBtn[idx].addEventListener("click", function() {
//         let count = parseInt(value[idx].textContent);
//         count--;
//         if (count < 2) {
//             minBtn[idx].setAttribute("disabled", "disabled")
//         }
//         value[idx].textContent = count;
//         total[idx].textContent = count * parseInt(price[0].textContent.trim().slice(2))
//     });




// })